/*code goes here
   Sends request through request pipe, recieves throuhgh reply pipe.

   Should use scanf() on three elements of the input from stdin.
   Request has three elements: int num1, char op, and int num2.
   User passes these to calc with reqPipe.
   Waits for result to come through rplyPipe, prints the final
   output to stdout.
*/
